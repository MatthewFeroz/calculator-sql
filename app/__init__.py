"""Application package for the calculator and its database-backed features.

Keeping application code inside a package gives tests, the FastAPI entry point,
and future modules one stable import path.  Module 10 adds the database, user,
schema, security, and service layers alongside the existing calculator logic.
"""

